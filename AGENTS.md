## Agent skills

### Issue tracker

Issues live in GitHub Issues (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical labels, strings equal to role names. See `docs/agents/triage-labels.md`.

### Build / compile

Do not compile to verify iterations. The user compiles the code
themselves on their own time. Just make the change and report it.

### Domain docs

Single-context layout (root `CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.
